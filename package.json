{
  "name": "neurokid",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "cd web && npm install",
    "build": "cd web && npx prisma generate && npm run build",
    "start": "cd web && npm start",
    "dev": "cd web && npm run dev"
  }
}
